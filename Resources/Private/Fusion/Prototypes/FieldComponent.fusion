prototype(Neos.Fusion.Form:FieldComponent) {
    @class = 'Neos\\Fusion\\Form\\FusionObjects\\FieldComponentImplementation'

    #
    # the values are used to populate the `field` context but are not available as `props
    #`
    field {
        form = ${form}
        name = null
        multiple = false
    }
}
