prototype(Neos.Fusion.Form:Textarea)  < prototype(Neos.Fusion.Form:FieldComponent) {

    renderer = afx`
        <textarea
            name={field.getName()}
            {...props.attributes}
        >
            {Form.stringifyValue(field.getValue() || props.content)}
        </textarea>
    `
}
