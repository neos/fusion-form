prototype(Neos.Fusion.Form:Textarea)  < prototype(Neos.Fusion.Form:Field) {
    renderer = afx`
        <textarea
            {...props.attributes}
            type="text"
            name={field.name}
            required={props.required}
        >{Form.stringifyValue(field.value)}</textarea>
    `
}
