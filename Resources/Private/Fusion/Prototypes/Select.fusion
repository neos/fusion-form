prototype(Neos.Fusion.Form:Select)  < prototype(Neos.Fusion.Form:FieldComponent) {

    renderer = afx`
        <select
            name={field.name}
            multiple={field.multiple}
            {...props.attributes}
        >
            {props.content}
        </select>
    `


}
