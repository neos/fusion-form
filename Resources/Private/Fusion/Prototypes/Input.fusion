prototype(Neos.Fusion.Form:Input)  < prototype(Neos.Fusion.Form:FieldComponent) {
    @propTypes {
        type = ${PropTypes.string}
    }

    type = 'text'

    renderer = afx`
        <input
            name={field.name}
            value={Form.stringifyValue(field.value || props.value)}
            {...props}
        />
    `
}
