prototype(Neos.Fusion.Form:Input)  < prototype(Neos.Fusion.Form:Field) {
    type = 'text'

    renderer = afx`
        <input
            {...props.attributes}
            type={props.type}
            name={field.name}
            value={Form.stringifyValue(props.value || field.value)}
            />
    `
}
