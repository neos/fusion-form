prototype(Neos.Fusion.Form:Select.Option)  < prototype(Neos.Fusion:Component) {
    selected = false
    value = false
    value.@process.stringify = ${Form.stringifyValue(value)}
    renderer = afx`
       <option value={props.value} selected={props.selected} >{props.content}</option>
    `
}
